import config
import pprint

FILE_VERSION = "1.2.3.4"

pprint.pprint(config.BUILD_VER)
pprint.pprint(config.COMMENTS)
pprint.pprint(config.FILE_VERSION)
pprint.pprint(config.TYPE_LONG)
pprint.pprint(config.TYPE_SHORT)
pprint.pprint(FILE_VERSION)
